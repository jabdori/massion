{
  "name": "@massion/distribution",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @massion/cli build && pnpm --filter @massion/connector build && pnpm --filter @massion/server build",
    "test": "node -e \"process.exit(0)\"",
    "typecheck": "node -e \"process.exit(0)\""
  },
  "dependencies": {
    "@massion/cli": "workspace:*",
    "@massion/connector": "workspace:*",
    "@massion/server": "workspace:*",
    "tslib": "2.8.1"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
