{
  "name": "@massion/runtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\" && tsc -p tsconfig.json",
    "test": "pnpm --filter @massion/identity build && pnpm --filter @massion/organization build && vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-sdk/provider-utils": "4.0.38",
    "@massion/identity": "workspace:*",
    "@massion/organization": "workspace:*",
    "@massion/router": "workspace:*",
    "@massion/storage": "workspace:*",
    "@ai-sdk/openai": "3.0.83",
    "@voltagent/core": "2.9.0",
    "@voltagent/logger": "2.0.2",
    "ai": "6.0.222",
    "zod": "4.4.3"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
